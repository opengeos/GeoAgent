# Factory

The factory module exposes `create_geo_agent` plus the per-package convenience helpers `for_leafmap`, `for_anymap`, and `for_qgis`. These build a deepagents-compiled agent ready to invoke.

::: geoagent.core.factory
